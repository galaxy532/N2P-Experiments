# Core
# torch is installed SEPARATELY by setup/setup_paperspace.sh, pinned to a CUDA build that
# matches the machine's GPU driver. Do NOT add `torch --index-url ...` here: in a requirements
# file --index-url is GLOBAL (it hijacks the index for every package), and an unpinned `torch`
# lets a plain `pip install -r` keep a base-image torch that's too new for the driver
# (e.g. 2.x+cu130 vs driver CUDA 12.4) -> torch.cuda.is_available()==False -> silent CPU run.
transformer_lens>=2.0      # HookedTransformer; supports gpt-j-6b and meta-llama Llama-3
transformers>=4.41
accelerate>=0.30
datasets>=2.19
huggingface_hub>=0.23

# Numerics / fitting
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3          # PCA, linear regression for helix fit
einops>=0.7

# Plotting / reporting
matplotlib>=3.7
pandas>=2.0
tqdm>=4.66

# SAEs (weeks 2-3; pinned here so the env is stable across the sprint)
# sae_lens>=3.0            # uncomment when starting feature-tracking week
